name: Publish NuGet Package

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Set up .NET SDK
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '6.x'  # Adjust the .NET SDK version

    - name: Restore Dependencies
      run: dotnet restore ./src/GithubActionsPractice/ExampleClassLibrary/

    - name: Build and Publish NuGet Package
      run: dotnet pack ./src/GithubActionsPractice/ExampleClassLibrary/ -c Release --output ./artifacts
      env:
        NUGET_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}

    - name: Publish NuGet Package
      run: dotnet nuget push ./artifacts/*.nupkg --source https://nuget.pkg.github.com/gnalvesteffer/index.json
      env:
        NUGET_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}
